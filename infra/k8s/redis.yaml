apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: nigeria-trust-redis
spec:
  serviceName: nigeria-trust-redis
  replicas: 1
  selector:
    matchLabels:
      app: nigeria-trust-redis
  template:
    metadata:
      labels:
        app: nigeria-trust-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          args: ["redis-server", "--appendonly", "yes"]
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: redis-data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: redis-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: nigeria-trust-redis
spec:
  selector:
    app: nigeria-trust-redis
  ports:
    - port: 6379
      targetPort: 6379
